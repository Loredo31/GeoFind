const MessageTemplates = require('./src/messageTemplates');

(async () => {
    try {
        const message = new MessageTemplates();

        const recipientEmail = "31loredo@gmail.com"; 
        const clientName = "Juan Pérez";
        const reason = "Horario no disponible";

        const [success, info] = await message.sendAppointmentRejection(recipientEmail, clientName, reason);
        console.log(success, info);
    } catch (err) {
        console.error(err);
    }
})();
