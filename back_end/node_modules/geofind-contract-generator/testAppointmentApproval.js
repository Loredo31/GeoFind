const MessageTemplates = require('./src/messageTemplates');

(async () => {
    try {
        const message = new MessageTemplates();

        const recipientEmail = "31loredo@gmail.com"; 
        const clientName = "Juan Pérez";
        const date = "18/10/2025";
        const time = "15:00";
        const location = "Oficina GeoFind, CDMX";

        const [success, info] = await message.sendAppointmentConfirmation(recipientEmail, clientName, date, time, location);
        console.log(success, info);
    } catch (err) {
        console.error(err);
    }
})();
